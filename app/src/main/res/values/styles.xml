<resources xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextViewPrimary" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/sizeTextPrimary</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="TextViewSecondary" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/sizeTextSecondary</item>
        <item name="android:textColor">@color/colorTextSecondary</item>
    </style>

    <style name="TextViewNotice" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/sizeTextMinimum</item>
        <item name="android:textColor">@color/colorTextNotice</item>
    </style>

    <style name="MyCardViewStyle" parent="CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="contentPadding">8dp</item>
    </style>

    <!--    Dialog negative button-->
    <style name="Widget.Sample.Button.ButtonBar.AlertDialog.Negative" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorTextSecondary</item>
    </style>

    <style name="ThemeOverlay.Sample.Dialog.Alert" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="buttonBarNegativeButtonStyle">
            @style/Widget.Sample.Button.ButtonBar.AlertDialog.Negative
        </item>
    </style>

    <!--    toolbar-->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <!--        <item name="android:background">@color/colorPrimary</item>-->
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
